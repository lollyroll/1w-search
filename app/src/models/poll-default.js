/*global define*/
define(
    'models/poll-default',
    [
		'backbone'
    ],
    function(Backbone) {

    	var pollDefault = Backbone.Model.extend({
    		// var model = this;

        });

        return pollDefault;
    }
    );